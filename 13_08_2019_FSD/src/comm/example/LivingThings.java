package comm.example;

public interface LivingThings {
    public static final String name= "HUMAN BEING";
    //the interface can have members but that attribute in interface can either be public or default and we 
    //make it static final so that we can access it from outside and its value cant be changed 
	
    public void walk();
    
}
